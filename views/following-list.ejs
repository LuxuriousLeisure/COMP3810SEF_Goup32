<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Clone - Following</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .list-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
        }
        .list-header {
            background: white;
            border: 1px solid #dbdbdb;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            text-align: center;
        }
        .list-header h1 {
            color: #262626;
            font-size: 24px;
            margin-bottom: 5px;
        }
        .list-header p {
            color: #8e8e8e;
            font-size: 14px;
        }
        .user-list {
            background: white;
            border: 1px solid #dbdbdb;
            border-radius: 8px;
            overflow: hidden;
        }
        .user-list-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 16px;
            border-bottom: 1px solid #efefef;
        }
        .user-list-item:last-child {
            border-bottom: none;
        }
        .user-list-info {
            display: flex;
            align-items: center;
            gap: 12px;
            flex: 1;
        }
        .user-list-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #dbdbdb;
        }
        .user-list-username {
            font-weight: 600;
            color: #262626;
            font-size: 14px;
        }
        .user-list-username a {
            color: #262626;
            text-decoration: none;
        }
        .user-list-username a:hover {
            color: #3897f0;
        }
        .btn-remove {
            padding: 8px 16px;
            background-color: #ed4956;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 13px;
            font-weight: bold;
        }
        .btn-remove:hover {
            background-color: #c92a35;
        }
        .btn-back {
            display: inline-block;
            margin-bottom: 20px;
            padding: 10px 20px;
            background-color: #dbdbdb;
            color: #262626;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }
        .btn-back:hover {
            background-color: #c7c7c7;
        }
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #8e8e8e;
        }
        .empty-state svg {
            width: 60px;
            height: 60px;
            margin-bottom: 20px;
            opacity: 0.5;
        }
    </style>
</head>
<body>
    <div class="main-container">
        <%- include('partials/navbar') %>
       
        <div class="content">
            <div class="list-container">
                <a href="/profile" class="btn-back">Back to Profile</a>
                <div class="list-header">
                    <h1>Following</h1>
                    <p><%= followingList.length %> account<%= followingList.length !== 1 ? 's' : '' %></p>
                </div>
                <div class="user-list">
                    <% if (followingList.length === 0) { %>
                        <div class="empty-state">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                            </svg>
                            <h3>Not following anyone yet</h3>
                            <p>Explore and follow other users!</p>
                        </div>
                    <% } else { %>
                        <% followingList.forEach(followingUser => { %>
                            <div class="user-list-item">
                                <div class="user-list-info">
                                    <img
                                        src="<%= followingUser.profileImage || '/images/default-avatar.jpg' %>"
                                        alt="<%= followingUser.username %>"
                                        class="user-list-avatar"
                                    >
                                    <div class="user-list-username">
                                        <a href="/users/<%= followingUser._id %>"><%= followingUser.username %></a>
                                    </div>
                                </div>
                                <form action="/following/<%= followingUser._id %>/unfollow" method="POST" style="margin: 0;">
                                    <button type="submit" class="btn-remove">Unfollow</button>
                                </form>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
